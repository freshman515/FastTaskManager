<Window x:Class="FastTaskManager.App.Views.CustomDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dialog"
        Width="456"
        SizeToContent="Height"
        MinHeight="248"
        MaxWidth="456"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <Style x:Key="DialogButtonBase" TargetType="Button">
            <Setter Property="Foreground" Value="{DynamicResource TextStrongBrush}"/>
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="18,11"/>
            <Setter Property="MinWidth" Value="110"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.78"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogSecondaryButton" TargetType="Button" BasedOn="{StaticResource DialogButtonBase}">
            <Setter Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="22"
          FocusManager.FocusedElement="{Binding ElementName=PrimaryActionButton}">
        <Border Background="{DynamicResource SurfaceBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="22">
            <Border.Effect>
                <DropShadowEffect Color="#000000"
                                  BlurRadius="34"
                                  ShadowDepth="12"
                                  Opacity="0.34"
                                  Direction="270"/>
            </Border.Effect>

            <Grid ClipToBounds="True">
                <Ellipse Width="220"
                         Height="220"
                         Fill="{Binding BadgeBackground}"
                         Opacity="0.16"
                         HorizontalAlignment="Right"
                         VerticalAlignment="Top"
                         Margin="0,-120,-80,0"/>

                <Grid Margin="24">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="18"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="20"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Width="54"
                                Height="54"
                                Background="{DynamicResource SurfaceAltBrush}"
                                BorderBrush="{Binding BadgeBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="16"
                                VerticalAlignment="Top">
                            <Grid>
                                <Border Width="30"
                                        Height="30"
                                        Background="{Binding BadgeBackground}"
                                        CornerRadius="15"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding IconGlyph}"
                                           FontFamily="Segoe MDL2 Assets"
                                           FontSize="19"
                                           Foreground="{Binding IconBrush}"
                                           VerticalAlignment="Center"
                                           HorizontalAlignment="Center"/>
                            </Grid>
                        </Border>

                        <StackPanel Grid.Column="2">
                            <Border HorizontalAlignment="Left"
                                    Padding="10,4"
                                    Background="{Binding BadgeBackground}"
                                    BorderBrush="{Binding BadgeBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="999">
                                <TextBlock Text="{Binding BadgeText}"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="{Binding BadgeForeground}"/>
                            </Border>

                            <TextBlock Margin="0,14,0,0"
                                       Text="{Binding DialogTitle}"
                                       FontSize="22"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource TextStrongBrush}"
                                       TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>

                    <Border Grid.Row="2"
                            Background="{DynamicResource SurfaceAltBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="16"
                            Padding="16,14">
                        <TextBlock Text="{Binding DialogMessage}"
                                   Foreground="{DynamicResource Muted2Brush}"
                                   FontSize="13"
                                   LineHeight="23"
                                   TextWrapping="Wrap"/>
                    </Border>

                    <Grid Grid.Row="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock VerticalAlignment="Center"
                                   Foreground="{DynamicResource MutedBrush}"
                                   FontSize="11"
                                   Text="此操作会立即生效"/>

                        <StackPanel Grid.Column="1"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Right">
                            <Button Content="{Binding SecondaryButtonText}"
                                     Style="{StaticResource DialogSecondaryButton}"
                                     Margin="0,0,10,0"
                                     IsCancel="True"
                                     Click="SecondaryButton_Click"
                                     Visibility="{Binding HasSecondaryButton, Converter={StaticResource BoolToVisibility}}"/>
                            <Button x:Name="PrimaryActionButton"
                                     Content="{Binding PrimaryButtonText}"
                                     Style="{StaticResource DialogButtonBase}"
                                     Background="{Binding PrimaryButtonBackground}"
                                     IsDefault="True"
                                     Click="PrimaryButton_Click"/>
                        </StackPanel>
                    </Grid>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
