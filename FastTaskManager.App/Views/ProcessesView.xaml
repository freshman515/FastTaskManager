<UserControl x:Class="FastTaskManager.App.Views.ProcessesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
   <UserControl.InputBindings>
       <KeyBinding Key="F5" Command="{Binding RefreshCommand}"/>
   </UserControl.InputBindings>
   <UserControl.Resources>
       <!-- 分类筛选 Tab 按钮样式 -->
        <Style x:Key="FilterTabStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="Margin" Value="0,0,6,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Root" Background="{TemplateBinding Background}"
                                CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="SelectedPill" Width="3" Height="16" Background="{DynamicResource AccentSecondaryBrush}" CornerRadius="2" Visibility="Collapsed" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                <ContentPresenter Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource AccentSoftBrush}"/>
                                <Setter TargetName="SelectedPill" Property="Visibility" Value="Visible"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextStrongBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{DynamicResource HoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
   </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="14" Padding="18,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="进程" Foreground="{DynamicResource TextBrush}" FontSize="22" FontWeight="SemiBold"/>
                    <TextBlock Text="保留原有进程管理逻辑，支持搜索、分组展开和右键操作。" Foreground="{DynamicResource MutedBrush}" FontSize="12" Margin="0,6,0,0"/>
                </StackPanel>
                <Border Grid.Column="1" Background="{DynamicResource PanelBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12,8">
                    <TextBlock Text="{Binding LastUpdatedText}" Foreground="{DynamicResource Muted2Brush}" FontSize="12"/>
                </Border>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Border Grid.Column="0" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16,12">
                <StackPanel><TextBlock Text="进程总数" Foreground="{DynamicResource MutedBrush}" FontSize="11" FontWeight="Medium"/><TextBlock Text="{Binding ProcessCount}" Foreground="{DynamicResource TextBrush}" FontSize="26" FontWeight="Bold" Margin="0,6,0,0"/></StackPanel>
            </Border>
            <Border Grid.Column="2" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16,12">
                <StackPanel><TextBlock Text="CPU 总占用" Foreground="{DynamicResource MutedBrush}" FontSize="11" FontWeight="Medium"/><TextBlock Text="{Binding TotalCpuText}" Foreground="{DynamicResource AccentBrush}" FontSize="26" FontWeight="Bold" Margin="0,6,0,0"/></StackPanel>
            </Border>
            <Border Grid.Column="4" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16,12">
                <StackPanel><TextBlock Text="内存占用" Foreground="{DynamicResource MutedBrush}" FontSize="11" FontWeight="Medium"/><TextBlock Text="{Binding TotalMemoryText}" Foreground="{DynamicResource TextBrush}" FontSize="26" FontWeight="Bold" Margin="0,6,0,0"/><TextBlock Text="{Binding TotalMemoryDetail}" Foreground="{DynamicResource Muted2Brush}" FontSize="12" Margin="0,3,0,0"/></StackPanel>
            </Border>
            <Border Grid.Column="6" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="16,12">
                <StackPanel>
                    <TextBlock Text="系统评估" Foreground="{DynamicResource MutedBrush}" FontSize="11" FontWeight="Medium"/>
                    <TextBlock Text="{Binding SystemAssessmentText}" Foreground="{Binding SystemAssessmentBrush}" FontSize="18" FontWeight="Bold" Margin="0,6,0,0" TextWrapping="Wrap"/>
                    <TextBlock Text="{Binding SystemAssessmentDetail}" Foreground="{DynamicResource Muted2Brush}" FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- 分类筛选 Tab 条 -->
        <Border Grid.Row="4" Background="{DynamicResource SurfaceAltBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="8,6">
            <StackPanel Orientation="Horizontal">
                <RadioButton GroupName="ProcessFilter" Style="{StaticResource FilterTabStyle}"
                             Content="全部" IsChecked="{Binding IsFilterAll, Mode=OneWay}"
                             Command="{Binding SetFilterCommand}" CommandParameter="All"/>
                <RadioButton GroupName="ProcessFilter" Style="{StaticResource FilterTabStyle}"
                             Content="{Binding AppTabText}" IsChecked="{Binding IsFilterApp, Mode=OneWay}"
                             Command="{Binding SetFilterCommand}" CommandParameter="App"/>
                <RadioButton GroupName="ProcessFilter" Style="{StaticResource FilterTabStyle}"
                             Content="{Binding BackgroundTabText}" IsChecked="{Binding IsFilterBackground, Mode=OneWay}"
                             Command="{Binding SetFilterCommand}" CommandParameter="Background"/>
                <RadioButton GroupName="ProcessFilter" Style="{StaticResource FilterTabStyle}"
                             Content="{Binding WindowsTabText}" IsChecked="{Binding IsFilterWindows, Mode=OneWay}"
                             Command="{Binding SetFilterCommand}" CommandParameter="Windows"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="6" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" ClipToBounds="True">
            <ListView x:Name="ProcessesListView" ItemsSource="{Binding DisplayItems}" SelectedItem="{Binding SelectedProcess}" AlternationCount="2" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextBrush}" HorizontalContentAlignment="Stretch" FontSize="13" ScrollViewer.CanContentScroll="True" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.ScrollUnit="Pixel" VirtualizingPanel.VirtualizationMode="Recycling" SizeChanged="ProcessesListView_SizeChanged">
                <ListView.ContextMenu>
                    <ContextMenu DataContext="{Binding Path=PlacementTarget.DataContext, RelativeSource={RelativeSource Self}}">
                        <MenuItem Header="结束进程" Command="{Binding KillProcessOnlyCommand}"/>
                        <MenuItem Header="结束进程树" Command="{Binding KillSelectedProcessCommand}"/>
                        <Separator/>
                        <MenuItem Header="暂停进程" Command="{Binding SuspendProcessCommand}"/>
                        <MenuItem Header="恢复进程" Command="{Binding ResumeProcessCommand}"/>
                        <Separator/>
                        <MenuItem Header="设置优先级">
                            <MenuItem Header="实时（RealTime）" Command="{Binding SetPriorityCommand}" CommandParameter="RealTime"/>
                            <MenuItem Header="高（High）" Command="{Binding SetPriorityCommand}" CommandParameter="High"/>
                            <MenuItem Header="高于正常" Command="{Binding SetPriorityCommand}" CommandParameter="AboveNormal"/>
                            <MenuItem Header="正常（默认）" Command="{Binding SetPriorityCommand}" CommandParameter="Normal"/>
                            <MenuItem Header="低于正常" Command="{Binding SetPriorityCommand}" CommandParameter="BelowNormal"/>
                            <MenuItem Header="低（Idle）" Command="{Binding SetPriorityCommand}" CommandParameter="Idle"/>
                        </MenuItem>
                        <Separator/>
                        <MenuItem Header="打开文件位置" Command="{Binding OpenFileLocationCommand}"/>
                        <Separator/>
                        <MenuItem Header="复制进程名" Command="{Binding CopyNameCommand}"/>
                        <MenuItem Header="复制 PID" Command="{Binding CopyPidCommand}"/>
                        <MenuItem Header="复制路径" Command="{Binding CopyPathCommand}"/>
                    </ContextMenu>
                </ListView.ContextMenu>

                <ListView.Resources>
                    <Style TargetType="GridViewColumnHeader">
                        <Setter Property="Background" Value="{DynamicResource PanelBrush}"/>
                        <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                        <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        <Setter Property="FontSize" Value="10.5"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                        <Setter Property="Padding" Value="14,11"/>
                        <Setter Property="HorizontalContentAlignment" Value="Left"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="GridViewColumnHeader">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}">
                                        <Grid>
                                            <Border Height="1" VerticalAlignment="Top" Background="{DynamicResource BorderBrush}" Opacity="0.85"/>
                                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                                              VerticalAlignment="Center"
                                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              RecognizesAccessKey="True"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <Style x:Key="GroupArrowButtonStyle" TargetType="Button">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="BorderBrush" Value="Transparent"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="ArrowRoot" Background="{TemplateBinding Background}" CornerRadius="7" Padding="1">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="ArrowRoot" Property="Background" Value="{DynamicResource HoverBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter TargetName="ArrowRoot" Property="Background" Value="{DynamicResource SelectedBrush}"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter TargetName="ArrowRoot" Property="Opacity" Value="0.45"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                    <Style TargetType="ListViewItem">
                        <EventSetter Event="PreviewMouseRightButtonDown" Handler="ListViewItem_SelectOnRightClick"/>
                        <EventSetter Event="MouseDoubleClick" Handler="ListViewItem_DoubleClick"/>
                        <Setter Property="Height" Value="38"/>
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border x:Name="RowRoot" Background="Transparent">
                                        <Grid>
                                            <Border x:Name="SelectLine" Width="3" HorizontalAlignment="Left" CornerRadius="0,2,2,0" Background="{DynamicResource AccentBrush}" Visibility="Collapsed"/>
                                            <GridViewRowPresenter x:Name="RowPresenter" Margin="3,0,0,0" VerticalAlignment="Center" Content="{TemplateBinding Content}" Columns="{Binding Path=View.Columns, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="ItemsControl.AlternationIndex" Value="1"><Setter TargetName="RowRoot" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/></Trigger>
                                        <DataTrigger Binding="{Binding IsGroup}" Value="True"><Setter TargetName="RowRoot" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/><Setter TargetName="RowPresenter" Property="TextElement.FontWeight" Value="SemiBold"/></DataTrigger>
                                        <DataTrigger Binding="{Binding IsChild}" Value="True"><Setter Property="Foreground" Value="{DynamicResource Muted2Brush}"/></DataTrigger>
                                        <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="RowRoot" Property="Background" Value="{DynamicResource HoverAltBrush}"/></Trigger>
                                        <Trigger Property="IsSelected" Value="True"><Setter TargetName="RowRoot" Property="Background" Value="{DynamicResource SelectedBrush}"/><Setter TargetName="SelectLine" Property="Visibility" Value="Visible"/></Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.Resources>

                <ListView.View>
                        <GridView x:Name="ProcessesGridView" AllowsColumnReorder="False">
                        <GridViewColumn x:Name="AlertColumn" Width="92" Header="告警">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Border x:Name="CpuAlert" Background="{DynamicResource ErrorBgBrush}" BorderBrush="{DynamicResource ErrorBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="5,1" Margin="0,0,4,0" Visibility="Collapsed">
                                            <TextBlock Text="CPU" Foreground="{DynamicResource ErrorTextBrush}" FontSize="9" FontWeight="SemiBold"/>
                                        </Border>
                                        <Border x:Name="MemoryAlert" Background="{DynamicResource InfoBgBrush}" BorderBrush="{DynamicResource InfoBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="5,1" Margin="0,0,4,0" Visibility="Collapsed">
                                            <TextBlock Text="MEM" Foreground="{DynamicResource AccentSecondaryBrush}" FontSize="9" FontWeight="SemiBold"/>
                                        </Border>
                                        <Border x:Name="ThreadAlert" Background="{DynamicResource WarningBgBrush}" BorderBrush="{DynamicResource WarningBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="5,1" Visibility="Collapsed">
                                            <TextBlock Text="THR" Foreground="{DynamicResource WarningBrush}" FontSize="9" FontWeight="SemiBold"/>
                                        </Border>
                                    </StackPanel>
                                    <DataTemplate.Triggers>
                                        <DataTrigger Binding="{Binding HasHighCpu}" Value="True"><Setter TargetName="CpuAlert" Property="Visibility" Value="Visible"/></DataTrigger>
                                        <DataTrigger Binding="{Binding HasHighMemory}" Value="True"><Setter TargetName="MemoryAlert" Property="Visibility" Value="Visible"/></DataTrigger>
                                        <DataTrigger Binding="{Binding HasHighThreads}" Value="True"><Setter TargetName="ThreadAlert" Property="Visibility" Value="Visible"/></DataTrigger>
                                        <DataTrigger Binding="{Binding IsGroup}" Value="True">
                                            <Setter TargetName="CpuAlert" Property="Visibility" Value="Collapsed"/>
                                            <Setter TargetName="MemoryAlert" Property="Visibility" Value="Collapsed"/>
                                            <Setter TargetName="ThreadAlert" Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </DataTemplate.Triggers>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn x:Name="NameColumn" Width="240" Header="名称">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Grid VerticalAlignment="Center">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="18"/>
                                            <ColumnDefinition Width="16"/>
                                            <ColumnDefinition Width="22"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <Border x:Name="Indent" Width="18" Grid.Column="0" Visibility="Collapsed"/>
                                        <Button x:Name="ArrowButton" Grid.Column="1" Width="14" Height="14" Padding="0" Margin="0" Background="Transparent" BorderThickness="0" Visibility="Collapsed" Click="GroupArrowButton_Click" Cursor="Hand" Focusable="False" Style="{StaticResource GroupArrowButtonStyle}">
                                            <TextBlock x:Name="Arrow" Text="&#xE76C;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                        </Button>
                                        <Image x:Name="ProcessIcon" Grid.Column="2" Source="{Binding Icon}" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                                        <TextBlock Grid.Column="3" Text="{Binding DisplayName}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                        <Border x:Name="TrayBadge" Grid.Column="4" Background="{DynamicResource SuccessBgBrush}" BorderBrush="{DynamicResource SuccessBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="6,1" Margin="8,0,0,0" VerticalAlignment="Center" Visibility="Collapsed">
                                            <TextBlock Text="{Binding TrayBadgeText}" Foreground="{DynamicResource SuccessBrush}" FontSize="10" FontWeight="SemiBold"/>
                                        </Border>
                                    </Grid>
                                     <DataTemplate.Triggers>
                                         <DataTrigger Binding="{Binding IsGroup}" Value="True"><Setter TargetName="ArrowButton" Property="Visibility" Value="Visible"/></DataTrigger>
                                         <DataTrigger Binding="{Binding IsExpanded}" Value="True"><Setter TargetName="Arrow" Property="Text" Value="&#xE70D;"/></DataTrigger>
                                         <DataTrigger Binding="{Binding IsChild}" Value="True"><Setter TargetName="Indent" Property="Visibility" Value="Visible"/></DataTrigger>
                                         <DataTrigger Binding="{Binding Icon}" Value="{x:Null}"><Setter TargetName="ProcessIcon" Property="Visibility" Value="Collapsed"/></DataTrigger>
                                         <DataTrigger Binding="{Binding IsTrayProcess}" Value="True"><Setter TargetName="TrayBadge" Property="Visibility" Value="Visible"/></DataTrigger>
                                     </DataTemplate.Triggers>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn x:Name="ImpactColumn" Width="86" Header="影响">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel>
                                        <TextBlock Text="{Binding ImpactScore}" Foreground="{DynamicResource TextBrush}" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding ImpactText}" Foreground="{DynamicResource MutedBrush}" FontSize="10" HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn x:Name="TypeColumn" Width="88" Header="类型" DisplayMemberBinding="{Binding CategoryText}"/>
                        <GridViewColumn x:Name="PidColumn" Width="70" Header="PID" DisplayMemberBinding="{Binding PidText}"/>
                        <GridViewColumn x:Name="CpuColumn" Width="90" Header="CPU">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding CpuText}" Foreground="{Binding CpuTextBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn x:Name="MemoryColumn" Width="110" Header="内存">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding MemoryText}" Foreground="{Binding MemoryTextBrush}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn x:Name="ThreadsColumn" Width="80" Header="线程" DisplayMemberBinding="{Binding ThreadText}"/>
                        <GridViewColumn x:Name="HandlesColumn" Width="80" Header="句柄" DisplayMemberBinding="{Binding HandleText}"/>
                        <GridViewColumn x:Name="StartTimeColumn" Width="100" Header="启动时间" DisplayMemberBinding="{Binding StartTimeText}"/>
                        <GridViewColumn x:Name="UptimeColumn" Width="110" Header="运行时长" DisplayMemberBinding="{Binding UptimeText}"/>
                        <GridViewColumn x:Name="PathColumn" Width="320" Header="路径" DisplayMemberBinding="{Binding PathText}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </Border>
    </Grid>
</UserControl>
